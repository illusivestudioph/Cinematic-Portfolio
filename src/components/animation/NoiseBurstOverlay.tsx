import React from 'react';

interface NoiseBurstOverlayProps {
  activeScene: string;
}

// Static burst disabled per user request — keeps footage clean and crystal clear
export const NoiseBurstOverlay: React.FC<NoiseBurstOverlayProps> = () => {
  return null;
};
